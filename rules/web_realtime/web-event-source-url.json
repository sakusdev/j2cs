{
  "id": "web.event-source.url",
  "category": "web_realtime",
  "summary": "EventSource url returns the serialized associated URL.",
  "source": {
    "pattern": "$source.url",
    "requirements": {
      "web_builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsRuntime.EventSourceState",
    "template": "JsRuntime.EventSourceState($args)",
    "notes": "Preserve browser networking, task/event dispatch, URL/WebIDL conversion, and protocol state rather than mapping to a raw socket."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "readonly": true,
    "value": "serialized URL"
  },
  "caveats": [],
  "references": [
    "https://html.spec.whatwg.org/multipage/server-sent-events.html"
  ],
  "tests": [
    {
      "name": "normal",
      "input": "source.url",
      "expected_behavior": "returns event stream URL"
    },
    {
      "name": "edge",
      "input": "redirected fetch",
      "expected_behavior": "MessageEvent origin uses final response URL while source.url remains associated URL semantics"
    }
  ]
}
