{
  "id": "uri.decode-uri-component.malformed",
  "category": "js_globals",
  "summary": "Preserve URIError for statically recognized malformed input to decodeURIComponent.",
  "source": {
    "pattern": "decodeURIComponent($string)",
    "requirements": {
      "binding_resolves_to_intrinsic": "%decodeURIComponent%",
      "builtin_not_replaced": true,
      "argument_static_type": "String",
      "input_statically_known_malformed_for_operation": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsUri.DecodeUriComponent",
    "template": "JsUri.DecodeUriComponent($string)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "decoding": "decode valid UTF-8 percent sequences including reserved delimiters",
    "preserve": "no URI-reserved preserve set",
    "errors": "malformed percent syntax or invalid UTF-8 scalar sequence throws URIError",
    "error": "URIError; no lossy replacement or permissive decoder"
  },
  "references": [
    "https://tc39.es/ecma262/2026/multipage/global-object.html"
  ],
  "tests": [
    {
      "name": "malformed",
      "input": "try{decodeURIComponent(\"%E0%A4%A\")}catch(e){console.log(e.name)}",
      "expected_behavior": "prints URIError"
    },
    {
      "name": "error category",
      "input": "try{decodeURIComponent(\"%E0%A4%A\")}catch(e){console.log(e instanceof URIError)}",
      "expected_behavior": "prints true"
    }
  ]
}
