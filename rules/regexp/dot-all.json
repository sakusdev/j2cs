{
  "id": "regexp.prototype.dotall",
  "category": "regexp",
  "summary": "Read the builtin RegExp dotAll accessor from stored ECMAScript flags.",
  "source": {
    "pattern": "$regexp.dotAll",
    "requirements": {
      "receiver_inferred_as": "builtin RegExp instance",
      "property_resolution": "builtin accessor proven unshadowed",
      "prototype_not_mutated": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsRegExp.GetDotAll",
    "template": "JsRegExp.GetDotAll($regexp)",
    "notes": "When property resolution is proven to the builtin accessor, return whether the original flag set contains s; the setter is undefined."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "flag": "s",
    "return": "boolean",
    "setter": "undefined",
    "brand_check": "RegExp internal OriginalFlags, except RegExp.prototype yields undefined when getter called directly"
  },
  "caveats": [
    "Own properties or prototype mutation can shadow/replace the builtin accessor; then generic property semantics are required."
  ],
  "references": [
    "https://tc39.es/ecma262/multipage/text-processing.html#sec-properties-of-the-regexp-prototype-object"
  ],
  "tests": [
    {
      "name": "dotAll true",
      "input": "console.log(/a/s.dotAll);",
      "expected_behavior": "prints true"
    },
    {
      "name": "prototype getter special case",
      "input": "const g=Object.getOwnPropertyDescriptor(RegExp.prototype,'dotAll').get; console.log(g.call(RegExp.prototype));",
      "expected_behavior": "prints undefined"
    }
  ]
}
