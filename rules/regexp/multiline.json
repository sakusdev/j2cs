{
  "id": "regexp.prototype.multiline",
  "category": "regexp",
  "summary": "Read the builtin RegExp multiline accessor from stored ECMAScript flags.",
  "source": {
    "pattern": "$regexp.multiline",
    "requirements": {
      "receiver_inferred_as": "builtin RegExp instance",
      "property_resolution": "builtin accessor proven unshadowed",
      "prototype_not_mutated": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsRegExp.GetMultiline",
    "template": "JsRegExp.GetMultiline($regexp)",
    "notes": "When property resolution is proven to the builtin accessor, return whether the original flag set contains m; the setter is undefined."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "flag": "m",
    "return": "boolean",
    "setter": "undefined",
    "brand_check": "RegExp internal OriginalFlags, except RegExp.prototype yields undefined when getter called directly"
  },
  "caveats": [
    "Own properties or prototype mutation can shadow/replace the builtin accessor; then generic property semantics are required."
  ],
  "references": [
    "https://tc39.es/ecma262/multipage/text-processing.html#sec-properties-of-the-regexp-prototype-object"
  ],
  "tests": [
    {
      "name": "multiline true",
      "input": "console.log(/a/m.multiline);",
      "expected_behavior": "prints true"
    },
    {
      "name": "prototype getter special case",
      "input": "const g=Object.getOwnPropertyDescriptor(RegExp.prototype,'multiline').get; console.log(g.call(RegExp.prototype));",
      "expected_behavior": "prints undefined"
    }
  ]
}
