{
  "id": "typescript.import-equals.qualified-alias",
  "category": "typescript_runtime",
  "summary": "Erase internal import-equals alias while preserving aliased value identity in uses.",
  "source": {
    "pattern": "import q = Namespace.member",
    "requirements": {
      "typescript_ast": true
    }
  },
  "target": {
    "kind": "helper",
    "notes": "Model observable JavaScript emitted by TypeScript first, then reuse canonical JS class/module/object semantics.",
    "template": "TsLowering.InternalImportAlias(q, Namespace.member)",
    "helper": "TsLowering.InternalImportAlias"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "emit": "no module load",
    "identity": "value uses refer to underlying value",
    "type_only": "type uses erase"
  },
  "caveats": [],
  "references": [
    "TypeScript Handbook: Enums / Namespaces",
    "TypeScript compiler emit semantics (profile-dependent)"
  ],
  "tests": [
    {
      "name": "alias",
      "input": "namespace N { export const x=3 }; import q=N.x; console.log(q);",
      "expected_behavior": "emitted use observes N.x value"
    },
    {
      "name": "no require",
      "input": "namespace N { export class C{} }; import Q=N.C;",
      "expected_behavior": "does not introduce module loading"
    }
  ]
}
