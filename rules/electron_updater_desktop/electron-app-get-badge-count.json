{
  "id": "electron.app.get-badge-count",
  "category": "electron.updater_desktop",
  "summary": "app.getBadgeCount returns the current badge counter on supported desktop platforms.",
  "source": {
    "pattern": "app.getBadgeCount()",
    "requirements": {
      "electron_builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "ElectronCompat.AppDesktop.GetBadgeCount",
    "template": "ElectronCompat.AppDesktop.GetBadgeCount($args)",
    "notes": "Preserve Electron host/platform behavior rather than substituting a superficially similar .NET API."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "return": "integer",
    "platform": "supported Electron host"
  },
  "caveats": [],
  "references": [
    "https://www.electronjs.org/docs/latest/api/app"
  ],
  "tests": [
    {
      "name": "normal",
      "input": "// Electron host test: app.getBadgeCount()",
      "expected_behavior": "counter returned"
    },
    {
      "name": "edge",
      "input": "// Electron semantic edge: app.getBadgeCount()",
      "expected_behavior": "must not derive by parsing dock badge text"
    }
  ]
}
