{
  "id": "electron.autoupdater.windows-updater-selection",
  "category": "electron.updater_desktop",
  "summary": "Windows autoUpdater selects MSIX or Squirrel behavior based on packaging.",
  "source": {
    "pattern": "autoUpdater on Windows",
    "requirements": {
      "electron_builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "runtime",
    "helper": "ElectronCompat.AutoUpdater.PlatformPreconditions",
    "template": "ElectronCompat.AutoUpdater.PlatformPreconditions($args)",
    "notes": "Preserve Electron host/platform behavior rather than substituting a superficially similar .NET API."
  },
  "strategy": "runtime",
  "confidence": "high",
  "semantics": {
    "platform": "win32",
    "selection": "MSIX when Windows Store/MSIX package, otherwise Squirrel.Windows packaging path"
  },
  "caveats": [],
  "references": [
    "https://www.electronjs.org/docs/latest/api/auto-updater"
  ],
  "tests": [
    {
      "name": "normal",
      "input": "// Electron host test: autoUpdater on Windows",
      "expected_behavior": "packaging determines mechanism"
    },
    {
      "name": "edge",
      "input": "// Electron semantic edge: autoUpdater on Windows",
      "expected_behavior": "do not hard-code one Windows updater"
    }
  ]
}
