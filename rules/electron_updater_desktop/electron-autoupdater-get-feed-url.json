{
  "id": "electron.autoupdater.get-feed-url",
  "category": "electron.updater_desktop",
  "summary": "getFeedURL returns the current configured update feed URL.",
  "source": {
    "pattern": "autoUpdater.getFeedURL()",
    "requirements": {
      "electron_builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "ElectronCompat.AutoUpdater.GetFeedURL",
    "template": "ElectronCompat.AutoUpdater.GetFeedURL($args)",
    "notes": "Preserve Electron host/platform behavior rather than substituting a superficially similar .NET API."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "return": "current feed URL string"
  },
  "caveats": [],
  "references": [
    "https://www.electronjs.org/docs/latest/api/auto-updater"
  ],
  "tests": [
    {
      "name": "normal",
      "input": "// Electron host test: autoUpdater.getFeedURL()",
      "expected_behavior": "returns configured URL"
    },
    {
      "name": "edge",
      "input": "// Electron semantic edge: autoUpdater.getFeedURL()",
      "expected_behavior": "does not perform a network check"
    }
  ]
}
