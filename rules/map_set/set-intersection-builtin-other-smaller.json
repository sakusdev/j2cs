{
  "id": "set.prototype.intersection.builtin-other-smaller",
  "category": "map_set",
  "summary": "intersection fast path when intrinsic other Set is smaller than the receiver.",
  "source": {
    "pattern": "$set.intersection($other)",
    "requirements": {
      "receiver_inferred_builtin_set": true,
      "builtin_member_not_overridden_or_shadowed": true,
      "receiver_not_proxy": true,
      "other_inferred_builtin_set": true,
      "other_set_protocol_intrinsic": true,
      "other_not_proxy": true,
      "size_relation": "receiver_size > other_reported_size"
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsSet.Intersection",
    "template": "JsSet.Intersection($set, $other)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "equality": "SameValueZero",
    "receiver_mutation": false,
    "result": "new ordinary Set",
    "path": "iterate other.keys() and test receiver SetData directly",
    "other_key_order": "observable where result is a Set",
    "iterator_close": "required on specified early-return branches"
  },
  "references": [
    "https://tc39.es/ecma262/2026/multipage/keyed-collections.html"
  ],
  "tests": [
    {
      "name": "other-side path",
      "input": "const a=new Set([1,2,3]); const b=new Set([2]); console.log([...a.intersection(b)]);",
      "expected_behavior": "uses other-keys branch with the specified result"
    },
    {
      "name": "other keys order",
      "input": "const a=new Set([3,2,1]); const b=new Set([1,3]); const r=a.intersection(b); console.log([...r]);",
      "expected_behavior": "preserves the branch-specific ordering contract"
    }
  ]
}
