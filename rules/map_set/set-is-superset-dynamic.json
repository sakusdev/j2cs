{
  "id": "set.prototype.is-superset.dynamic-setlike",
  "category": "map_set",
  "summary": "Use runtime GetSetRecord/call/iterator semantics for dynamic isSupersetOf.",
  "source": {
    "pattern": "$set.isSupersetOf($other)",
    "requirements": {
      "receiver_inferred_builtin_set": true,
      "builtin_member_not_overridden_or_shadowed": true,
      "receiver_not_proxy": true,
      "static_set_like_shape_not_proven": true
    }
  },
  "target": {
    "kind": "runtime",
    "helper": "JsRuntime.SetIsSupersetOf",
    "template": "JsRuntime.SetIsSupersetOf($set, $other)"
  },
  "strategy": "runtime",
  "confidence": "high",
  "semantics": {
    "result": "Boolean",
    "get_set_record": "observable and throwable",
    "branch": "reported size controls whether has or keys is invoked",
    "iterator_close": "preserved on keys-path early false",
    "receiver_reentrancy": "observable where has can mutate receiver"
  },
  "references": [
    "https://tc39.es/ecma262/2026/multipage/keyed-collections.html"
  ],
  "tests": [
    {
      "name": "protocol getter order",
      "input": "const log=[]; const o={get size(){log.push('s');return 0},get has(){log.push('h');return ()=>false},get keys(){log.push('k');return function*(){}}}; new Set().isSupersetOf(o); console.log(log.join(''));",
      "expected_behavior": "prints shk"
    },
    {
      "name": "size coercion can throw",
      "input": "const o={size:{valueOf(){throw 'x'}},has(){},keys(){}}; try{new Set().isSupersetOf(o)}catch(e){console.log(e)}",
      "expected_behavior": "prints x"
    }
  ]
}
