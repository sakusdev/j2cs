{
  "id": "node.fs.read-stream.start-end",
  "category": "node_fs_watch_streams",
  "summary": "Read inclusive byte range.",
  "source": {
    "pattern": "fs.createReadStream(path,{start,end})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.CreateReadStream",
    "template": "NodeFs.CreateReadStream(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "end is inclusive, unlike many range APIs."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.createReadStream(path,{start,end})",
      "expected_behavior": "Read inclusive byte range."
    },
    {
      "name": "semantic trap",
      "input": "fs.createReadStream(path,{start,end})",
      "expected_behavior": "end is inclusive, unlike many range APIs."
    }
  ]
}
