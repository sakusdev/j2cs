{
  "id": "node.fs.read-stream.flags",
  "category": "node_fs_watch_streams",
  "summary": "Preserve Node open flags.",
  "source": {
    "pattern": "fs.createReadStream(path,{flags})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.CreateReadStream",
    "template": "NodeFs.CreateReadStream(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Default is 'r'; errors/codes follow NODE_FS."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.createReadStream(path,{flags})",
      "expected_behavior": "Preserve Node open flags."
    },
    {
      "name": "semantic trap",
      "input": "fs.createReadStream(path,{flags})",
      "expected_behavior": "Default is 'r'; errors/codes follow NODE_FS."
    }
  ]
}
