{
  "id": "node.fs.watch.watch-file",
  "category": "node_fs_watch_streams",
  "summary": "Use stat polling rather than fs.watch backend.",
  "source": {
    "pattern": "fs.watchFile(path,options,listener)",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.WatchFile",
    "template": "NodeFs.WatchFile(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Polling has different performance/reliability and default interval 5007ms."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watchFile(path,options,listener)",
      "expected_behavior": "Use stat polling rather than fs.watch backend."
    },
    {
      "name": "semantic trap",
      "input": "fs.watchFile(path,options,listener)",
      "expected_behavior": "Polling has different performance/reliability and default interval 5007ms."
    }
  ]
}
