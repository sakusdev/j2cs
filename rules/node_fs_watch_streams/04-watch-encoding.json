{
  "id": "node.fs.watch.encoding",
  "category": "node_fs_watch_streams",
  "summary": "Preserve filename string/Buffer encoding.",
  "source": {
    "pattern": "fs.watch(path,{encoding})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "encoding:'buffer' yields Buffer filenames when available."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watch(path,{encoding})",
      "expected_behavior": "Preserve filename string/Buffer encoding."
    },
    {
      "name": "semantic trap",
      "input": "fs.watch(path,{encoding})",
      "expected_behavior": "encoding:'buffer' yields Buffer filenames when available."
    }
  ]
}
