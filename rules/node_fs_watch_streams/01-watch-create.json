{
  "id": "node.fs.watch.create",
  "category": "node_fs_watch_streams",
  "summary": "Create an FSWatcher using the host watch backend.",
  "source": {
    "pattern": "fs.watch(path, options, listener)",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "OS backend/availability and network-filesystem unreliability are observable."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watch(path, options, listener)",
      "expected_behavior": "Create an FSWatcher using the host watch backend."
    },
    {
      "name": "semantic trap",
      "input": "fs.watch(path, options, listener)",
      "expected_behavior": "OS backend/availability and network-filesystem unreliability are observable."
    }
  ]
}
