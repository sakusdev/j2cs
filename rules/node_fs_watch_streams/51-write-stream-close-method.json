{
  "id": "node.fs.write-stream.close-method",
  "category": "node_fs_watch_streams",
  "summary": "Close fd after flushing stream rules.",
  "source": {
    "pattern": "stream.close(callback)",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.WriteStreamCloseMethod",
    "template": "NodeFs.WriteStreamCloseMethod(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Must preserve callback/error ordering and writable lifecycle."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "stream.close(callback)",
      "expected_behavior": "Close fd after flushing stream rules."
    },
    {
      "name": "semantic trap",
      "input": "stream.close(callback)",
      "expected_behavior": "Must preserve callback/error ordering and writable lifecycle."
    }
  ]
}
