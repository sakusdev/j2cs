{
  "id": "node.fs.opendir.recursive",
  "category": "node_fs_watch_streams",
  "summary": "Recursively iterate subdirectories when supported by Node option.",
  "source": {
    "pattern": "opendir(path,{recursive:true})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.OpenDir",
    "template": "NodeFs.OpenDir(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Returned entries/path ordering remains Node-defined host traversal."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "opendir(path,{recursive:true})",
      "expected_behavior": "Recursively iterate subdirectories when supported by Node option."
    },
    {
      "name": "semantic trap",
      "input": "opendir(path,{recursive:true})",
      "expected_behavior": "Returned entries/path ordering remains Node-defined host traversal."
    }
  ]
}
