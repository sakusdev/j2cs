{
  "id": "node.fs.read-stream.ready-event",
  "category": "node_fs_watch_streams",
  "summary": "Emit ready immediately after open phase.",
  "source": {
    "pattern": "stream.on('ready',()=>{})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.ReadStreamReady",
    "template": "NodeFs.ReadStreamReady(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Ordering relative to open must match Node."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "stream.on('ready',()=>{})",
      "expected_behavior": "Emit ready immediately after open phase."
    },
    {
      "name": "semantic trap",
      "input": "stream.on('ready',()=>{})",
      "expected_behavior": "Ordering relative to open must match Node."
    }
  ]
}
