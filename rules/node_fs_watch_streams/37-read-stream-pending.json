{
  "id": "node.fs.read-stream.pending",
  "category": "node_fs_watch_streams",
  "summary": "Read whether underlying fd is not yet open.",
  "source": {
    "pattern": "stream.pending",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.ReadStreamPending",
    "template": "NodeFs.ReadStreamPending(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "False after fd is available/stream lifecycle advances."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "stream.pending",
      "expected_behavior": "Read whether underlying fd is not yet open."
    },
    {
      "name": "semantic trap",
      "input": "stream.pending",
      "expected_behavior": "False after fd is available/stream lifecycle advances."
    }
  ]
}
