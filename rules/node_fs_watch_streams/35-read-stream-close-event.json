{
  "id": "node.fs.read-stream.close-event",
  "category": "node_fs_watch_streams",
  "summary": "Emit close when underlying resource closes if enabled.",
  "source": {
    "pattern": "stream.on('close',()=>{})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.ReadStreamClose",
    "template": "NodeFs.ReadStreamClose(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Generic stream close mechanics reused from EVENTS_STREAMS."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "stream.on('close',()=>{})",
      "expected_behavior": "Emit close when underlying resource closes if enabled."
    },
    {
      "name": "semantic trap",
      "input": "stream.on('close',()=>{})",
      "expected_behavior": "Generic stream close mechanics reused from EVENTS_STREAMS."
    }
  ]
}
