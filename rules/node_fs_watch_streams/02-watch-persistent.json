{
  "id": "node.fs.watch.persistent",
  "category": "node_fs_watch_streams",
  "summary": "Preserve whether watcher keeps the event loop alive.",
  "source": {
    "pattern": "fs.watch(path,{persistent})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "persistent defaults true."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watch(path,{persistent})",
      "expected_behavior": "Preserve whether watcher keeps the event loop alive."
    },
    {
      "name": "semantic trap",
      "input": "fs.watch(path,{persistent})",
      "expected_behavior": "persistent defaults true."
    }
  ]
}
