{
  "id": "node.fs.watch.event-type",
  "category": "node_fs_watch_streams",
  "summary": "Deliver Node 'rename'/'change' eventType values.",
  "source": {
    "pattern": "watch(path,(eventType,filename)=>{})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Listener 'change' event is distinct from eventType value 'change'."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "watch(path,(eventType,filename)=>{})",
      "expected_behavior": "Deliver Node 'rename'/'change' eventType values."
    },
    {
      "name": "semantic trap",
      "input": "watch(path,(eventType,filename)=>{})",
      "expected_behavior": "Listener 'change' event is distinct from eventType value 'change'."
    }
  ]
}
