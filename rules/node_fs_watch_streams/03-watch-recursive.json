{
  "id": "node.fs.watch.recursive",
  "category": "node_fs_watch_streams",
  "summary": "Request recursive directory watching on supported platforms.",
  "source": {
    "pattern": "fs.watch(path,{recursive:true})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Recursive support is platform/version dependent."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watch(path,{recursive:true})",
      "expected_behavior": "Request recursive directory watching on supported platforms."
    },
    {
      "name": "semantic trap",
      "input": "fs.watch(path,{recursive:true})",
      "expected_behavior": "Recursive support is platform/version dependent."
    }
  ]
}
