{
  "id": "node.fs.read-stream.fd",
  "category": "node_fs_watch_streams",
  "summary": "Use supplied file descriptor and fd ownership options.",
  "source": {
    "pattern": "fs.createReadStream(path,{fd})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.CreateReadStream",
    "template": "NodeFs.CreateReadStream(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "When fd is supplied, path is ignored except diagnostics and autoClose controls ownership."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.createReadStream(path,{fd})",
      "expected_behavior": "Use supplied file descriptor and fd ownership options."
    },
    {
      "name": "semantic trap",
      "input": "fs.createReadStream(path,{fd})",
      "expected_behavior": "When fd is supplied, path is ignored except diagnostics and autoClose controls ownership."
    }
  ]
}
