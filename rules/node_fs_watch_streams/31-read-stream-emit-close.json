{
  "id": "node.fs.read-stream.emit-close",
  "category": "node_fs_watch_streams",
  "summary": "Preserve close event suppression option.",
  "source": {
    "pattern": "fs.createReadStream(path,{emitClose:false})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.CreateReadStream",
    "template": "NodeFs.CreateReadStream(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Resource closure and close-event emission are distinct."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.createReadStream(path,{emitClose:false})",
      "expected_behavior": "Preserve close event suppression option."
    },
    {
      "name": "semantic trap",
      "input": "fs.createReadStream(path,{emitClose:false})",
      "expected_behavior": "Resource closure and close-event emission are distinct."
    }
  ]
}
