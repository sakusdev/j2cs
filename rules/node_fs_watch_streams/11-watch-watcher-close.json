{
  "id": "node.fs.watch.watcher-close",
  "category": "node_fs_watch_streams",
  "summary": "Stop watching and emit close semantics.",
  "source": {
    "pattern": "watcher.close()",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.FSWatcherClose",
    "template": "NodeFs.FSWatcherClose(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Closed watcher is no longer usable."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "watcher.close()",
      "expected_behavior": "Stop watching and emit close semantics."
    },
    {
      "name": "semantic trap",
      "input": "watcher.close()",
      "expected_behavior": "Closed watcher is no longer usable."
    }
  ]
}
