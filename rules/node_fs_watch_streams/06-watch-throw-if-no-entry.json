{
  "id": "node.fs.watch.throw-if-no-entry",
  "category": "node_fs_watch_streams",
  "summary": "Preserve missing-entry throw/suppression behavior.",
  "source": {
    "pattern": "fs.watch(path,{throwIfNoEntry:false})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.Watch",
    "template": "NodeFs.Watch(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Modern Node can suppress missing-path exception with throwIfNoEntry:false."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.watch(path,{throwIfNoEntry:false})",
      "expected_behavior": "Preserve missing-entry throw/suppression behavior."
    },
    {
      "name": "semantic trap",
      "input": "fs.watch(path,{throwIfNoEntry:false})",
      "expected_behavior": "Modern Node can suppress missing-path exception with throwIfNoEntry:false."
    }
  ]
}
