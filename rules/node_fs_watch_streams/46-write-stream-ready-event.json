{
  "id": "node.fs.write-stream.ready-event",
  "category": "node_fs_watch_streams",
  "summary": "Emit ready after open phase.",
  "source": {
    "pattern": "stream.on('ready',()=>{})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.WriteStreamReady",
    "template": "NodeFs.WriteStreamReady(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Preserve Node open/ready ordering."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "stream.on('ready',()=>{})",
      "expected_behavior": "Emit ready after open phase."
    },
    {
      "name": "semantic trap",
      "input": "stream.on('ready',()=>{})",
      "expected_behavior": "Preserve Node open/ready ordering."
    }
  ]
}
