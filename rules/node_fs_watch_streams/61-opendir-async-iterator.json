{
  "id": "node.fs.opendir.async-iterator",
  "category": "node_fs_watch_streams",
  "summary": "Iterate Dirents asynchronously and auto-close on completion.",
  "source": {
    "pattern": "dir[Symbol.asyncIterator]()",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.DirAsyncIterator",
    "template": "NodeFs.DirAsyncIterator(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Breaking/throwing must close directory via iterator-close semantics."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "dir[Symbol.asyncIterator]()",
      "expected_behavior": "Iterate Dirents asynchronously and auto-close on completion."
    },
    {
      "name": "semantic trap",
      "input": "dir[Symbol.asyncIterator]()",
      "expected_behavior": "Breaking/throwing must close directory via iterator-close semantics."
    }
  ]
}
