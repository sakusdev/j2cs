{
  "id": "node.fs.write-stream.fd",
  "category": "node_fs_watch_streams",
  "summary": "Use supplied descriptor with Node ownership semantics.",
  "source": {
    "pattern": "fs.createWriteStream(path,{fd})",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.CreateWriteStream",
    "template": "NodeFs.CreateWriteStream(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Path/open phase can be bypassed when fd supplied."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "fs.createWriteStream(path,{fd})",
      "expected_behavior": "Use supplied descriptor with Node ownership semantics."
    },
    {
      "name": "semantic trap",
      "input": "fs.createWriteStream(path,{fd})",
      "expected_behavior": "Path/open phase can be bypassed when fd supplied."
    }
  ]
}
