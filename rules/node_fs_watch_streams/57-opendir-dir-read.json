{
  "id": "node.fs.opendir.dir-read",
  "category": "node_fs_watch_streams",
  "summary": "Read next Dirent asynchronously.",
  "source": {
    "pattern": "dir.read(callback)",
    "requirements": {
      "node_builtin_binding": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeFs.DirRead",
    "template": "NodeFs.DirRead(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "node_host_semantics": true,
    "events": "reuse EVENTS_STREAMS where generic",
    "path_errors": "reuse NODE_FS core"
  },
  "caveats": [
    "Returns null at end; concurrent reads/closed dir errors preserved."
  ],
  "references": [
    "https://nodejs.org/api/fs.html"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "dir.read(callback)",
      "expected_behavior": "Read next Dirent asynchronously."
    },
    {
      "name": "semantic trap",
      "input": "dir.read(callback)",
      "expected_behavior": "Returns null at end; concurrent reads/closed dir errors preserved."
    }
  ]
}
