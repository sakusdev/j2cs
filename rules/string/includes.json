{
  "id": "string.prototype.includes",
  "category": "string",
  "summary": "Lower includes with JavaScript coercion, clamping, and UTF-16 ordinal matching.",
  "source": {
    "pattern": "$string.includes($search, $position)",
    "requirements": {
      "receiver_inferred_as": "builtin String primitive",
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsString.Includes",
    "template": "JsString.Includes($string, $search, $position)",
    "notes": "Must apply JavaScript coercion/clamping and ordinal UTF-16 matching; do not use culture-sensitive .NET defaults."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "search": "code-unit exact ordinal substring",
    "position": "ToIntegerOrInfinity then clamp to 0..length",
    "regexp": "RegExp searchString throws TypeError"
  },
  "caveats": [
    "C# string search overloads can be culture-sensitive unless StringComparison.Ordinal is selected."
  ],
  "references": [
    "https://tc39.es/ecma262/#sec-string-objects"
  ],
  "tests": [
    {
      "name": "basic match",
      "input": "console.log(\"abcabc\".includes(\"bc\"));",
      "expected_csharp": "Console.WriteLine(JsString.Includes(\"abcabc\", \"bc\", JsUndefined.Value));",
      "expected_behavior": "matches according to JavaScript semantics"
    },
    {
      "name": "edge position",
      "input": "console.log(\"abc\".includes(\"\", 99));",
      "expected_csharp": "Console.WriteLine(JsString.Includes(\"abc\", \"\", 99d));",
      "expected_behavior": "prints true"
    }
  ]
}
