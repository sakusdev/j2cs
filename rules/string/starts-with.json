{
  "id": "string.prototype.startswith",
  "category": "string",
  "summary": "Lower startsWith with JavaScript coercion, clamping, and UTF-16 ordinal matching.",
  "source": {
    "pattern": "$string.startsWith($search, $position)",
    "requirements": {
      "receiver_inferred_as": "builtin String primitive",
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsString.StartsWith",
    "template": "JsString.StartsWith($string, $search, $position)",
    "notes": "Must apply JavaScript coercion/clamping and ordinal UTF-16 matching; do not use culture-sensitive .NET defaults."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "search": "code-unit exact ordinal match",
    "position": "ToIntegerOrInfinity then clamp",
    "regexp": "RegExp searchString throws TypeError"
  },
  "caveats": [
    "C# string search overloads can be culture-sensitive unless StringComparison.Ordinal is selected."
  ],
  "references": [
    "https://tc39.es/ecma262/#sec-string-objects"
  ],
  "tests": [
    {
      "name": "basic match",
      "input": "console.log(\"abcabc\".startsWith(\"bc\"));",
      "expected_csharp": "Console.WriteLine(JsString.StartsWith(\"abcabc\", \"bc\", JsUndefined.Value));",
      "expected_behavior": "matches according to JavaScript semantics"
    },
    {
      "name": "edge position",
      "input": "console.log(\"abc\".startsWith(\"b\", 1));",
      "expected_csharp": "Console.WriteLine(JsString.StartsWith(\"abc\", \"b\", 1d));",
      "expected_behavior": "prints true"
    }
  ]
}
