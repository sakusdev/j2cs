{
  "id": "web.indexeddb.cursor.continue-primary-key",
  "category": "web_indexeddb",
  "summary": "Advance index cursor by key and primary key",
  "source": {
    "pattern": "cursor.continuePrimaryKey(key,primaryKey)",
    "requirements": {
      "intrinsic_indexeddb_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsIndexedDb.CursorContinuePrimaryKey",
    "template": "JsIndexedDb.CursorContinuePrimaryKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "request_model": "event-driven IDBRequest lifecycle",
    "transaction_lifetime": "task-sensitive active/inactive state",
    "structured_clone": "values preserve IndexedDB structured-clone behavior"
  },
  "caveats": [
    "Operations remain bound to the originating IndexedDB transaction and origin."
  ],
  "references": [
    "https://w3c.github.io/IndexedDB/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "cursor.continuePrimaryKey(key,primaryKey)",
      "expected_behavior": "Advance index cursor by key and primary key"
    },
    {
      "name": "edge semantics",
      "input": "cursor.continuePrimaryKey(key,primaryKey)",
      "expected_behavior": "Operations remain bound to the originating IndexedDB transaction and origin."
    }
  ]
}
