{
  "id": "web.indexeddb.index.get-all-keys",
  "category": "web_indexeddb",
  "summary": "Retrieve matching primary keys",
  "source": {
    "pattern": "index.getAllKeys(query,count)",
    "requirements": {
      "intrinsic_indexeddb_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsIndexedDb.IndexGetAllKeys",
    "template": "JsIndexedDb.IndexGetAllKeys(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "request_model": "event-driven IDBRequest lifecycle",
    "transaction_lifetime": "task-sensitive active/inactive state",
    "structured_clone": "values preserve IndexedDB structured-clone behavior"
  },
  "caveats": [
    "Operations remain bound to the originating IndexedDB transaction and origin."
  ],
  "references": [
    "https://w3c.github.io/IndexedDB/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "index.getAllKeys(query,count)",
      "expected_behavior": "Retrieve matching primary keys"
    },
    {
      "name": "edge semantics",
      "input": "index.getAllKeys(query,count)",
      "expected_behavior": "Operations remain bound to the originating IndexedDB transaction and origin."
    }
  ]
}
