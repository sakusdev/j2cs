{
  "id": "web.readable-stream.get-reader.byob",
  "category": "web_streams",
  "summary": "Acquire a BYOB reader from a readable byte stream.",
  "source": {
    "pattern": "$stream.getReader({mode:'byob'})",
    "requirements": {
      "intrinsic_web_stream_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebStreams.GetByobReader",
    "template": "JsWebStreams.GetByobReader(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "normal": "locks a byte stream and returns a BYOB reader",
    "edge": "throws TypeError for a non-byte stream"
  },
  "caveats": [],
  "references": [
    "https://streams.spec.whatwg.org/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "new ReadableStream({type:'bytes'}).getReader({mode:'byob'})",
      "expected_behavior": "locks a byte stream and returns a BYOB reader"
    },
    {
      "name": "edge semantics",
      "input": "new ReadableStream().getReader({mode:'byob'})",
      "expected_behavior": "throws TypeError for a non-byte stream"
    }
  ]
}
