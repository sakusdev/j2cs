{
  "id": "date.prototype.sethours",
  "category": "date_time",
  "summary": "setHours mutates the Date: replace local hour and optional lower fields; omitted fields come from current local time; it returns the resulting millisecond time value.",
  "source": {
    "pattern": "$date.setHours($hours, $minutes?, $seconds?, $ms?)",
    "requirements": {
      "receiver_has_date_value_internal_slot": true,
      "property_lookup_resolves_builtin": "Date.prototype.setHours"
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsDate.setHours",
    "template": "JsDate.setHours($date, ...)",
    "notes": "Reuse canonical ToNumber helpers/runtime as required by argument static types; preserve mutation only after the specified conversions/calculations."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "mutates_receiver": true,
    "return": "new [[DateValue]] after TimeClip, or NaN",
    "coercion": "ToNumber supplied arguments in specification order",
    "timezone": "host local",
    "description": "replace local hour and optional lower fields; omitted fields come from current local time",
    "invalid_receiver_recovery": false
  },
  "caveats": [
    "Explicit undefined is a supplied argument and ToNumber(undefined) is NaN; omitted optional arguments instead use current component defaults.",
    "Local setters are DST-sensitive; elapsed milliseconds can differ from nominal component changes."
  ],
  "references": [
    "https://tc39.es/ecma262/multipage/numbers-and-dates.html#sec-date-objects",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date"
  ],
  "tests": [
    {
      "name": "valid mutation returns time value",
      "input": "const d=new Date(2024,0,15,1,2,3,4); d.setHours(10); console.log(d.getHours(),d.getMinutes());",
      "expected_behavior": "prints 10 2"
    },
    {
      "name": "semantic edge case",
      "input": "const d=new Date(2024,0,15); console.log(Number.isNaN(d.setHours(1,undefined)));",
      "expected_behavior": "prints true"
    }
  ]
}
