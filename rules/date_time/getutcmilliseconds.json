{
  "id": "date.prototype.getutcmilliseconds",
  "category": "date_time",
  "summary": "getUTCMilliseconds reads UTC milliseconds 0..999 directly from the UTC time value; Invalid Date returns NaN.",
  "source": {
    "pattern": "$date.getUTCMilliseconds()",
    "requirements": {
      "receiver_has_date_value_internal_slot": true,
      "property_lookup_resolves_builtin": "Date.prototype.getUTCMilliseconds"
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsDate.getUTCMilliseconds",
    "template": "JsDate.getUTCMilliseconds($date)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "receiver_mutated": false,
    "invalid_date": "NaN",
    "timezone": "UTC",
    "operation": "msFromTime"
  },
  "caveats": [],
  "references": [
    "https://tc39.es/ecma262/multipage/numbers-and-dates.html#sec-date-objects",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date"
  ],
  "tests": [
    {
      "name": "valid UTC component",
      "input": "console.log(new Date(Date.UTC(2024,0,1,0,0,0,678)).getUTCMilliseconds());",
      "expected_behavior": "prints 678"
    },
    {
      "name": "invalid Date returns NaN",
      "input": "console.log(Number.isNaN(new Date(NaN).getUTCMilliseconds()));",
      "expected_behavior": "prints true"
    }
  ]
}
