{
  "id": "electron.webcontents.navigation.history-can-go-back",
  "category": "electron_webcontents_advanced",
  "summary": "Query navigationHistory for an available previous entry.",
  "source": {
    "pattern": "$contents.navigationHistory.canGoBack()",
    "requirements": {
      "receiver": "Electron WebContents",
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "runtime",
    "notes": "Preserve Electron WebContents/Chromium renderer semantics through the ElectronCompat host boundary; do not replace navigation, rendering, printing, or DevTools with ordinary .NET calls.",
    "template": "ElectronCompat.NavigationHistoryCanGoBack($contents)"
  },
  "strategy": "runtime",
  "confidence": "high",
  "semantics": {
    "return_value": "boolean",
    "history": "uses WebContents navigation history, preferred over deprecated contents.canGoBack()"
  },
  "caveats": [],
  "references": [
    "https://www.electronjs.org/docs/latest/api/web-contents"
  ],
  "tests": [
    {
      "name": "initial",
      "input": "console.log(wc.navigationHistory.canGoBack())",
      "expected_behavior": "false for a fresh history with no prior entry"
    },
    {
      "name": "after two navigations",
      "input": "await wc.loadURL(a);await wc.loadURL(b);console.log(wc.navigationHistory.canGoBack())",
      "expected_behavior": "typically true"
    }
  ]
}
