{
  "id": "math.fround",
  "category": "math_number",
  "summary": "Round to IEEE-754 binary32 using roundTiesToEven and widen back to binary64.",
  "source": {
    "pattern": "Math.fround($x)",
    "requirements": {
      "binding_resolves_to_intrinsic": "%Math%",
      "method_resolution_intrinsic": true,
      "to_number_lowering": "reuse COERCION"
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsMath.Fround",
    "template": "JsMath.Fround($x)",
    "notes": "Compose with canonical COERCION ToNumber before the numeric kernel; if ToNumber requires runtime, the whole expression inherits that dependency."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "argument_conversion": "ECMAScript ToNumber via canonical COERCION rules",
    "format": "binary32 then binary64",
    "rounding": "roundTiesToEven",
    "negative_zero_nan_infinity": "preserved"
  },
  "caveats": [
    "Do not use decimal rounding; this is an IEEE binary32 conversion."
  ],
  "references": [
    "https://tc39.es/ecma262/2026/multipage/numbers-and-dates.html"
  ],
  "tests": [
    {
      "name": "representative",
      "input": "console.log(Math.fround(1.337));",
      "expected_behavior": "prints the binary32-rounded value widened to Number"
    },
    {
      "name": "negative zero",
      "input": "console.log(Object.is(Math.fround(-0),-0));",
      "expected_behavior": "prints true"
    }
  ]
}
