{
  "id": "node.http.server-response.set-header",
  "category": "node_http_crypto",
  "summary": "Set or replace an implicit response header.",
  "source": {
    "pattern": "$response.setHeader($name, $value)",
    "requirements": {
      "receiver_inferred_as": "node:http ServerResponse",
      "builtin_method_or_property_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeHttp.ResponseSetHeader",
    "template": "NodeHttp.ResponseSetHeader($response, $name, $value)",
    "notes": "Returns receiver; lookup case-insensitive; number/string/string[] stored until transmission; invalid names/values throw."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "notes": "Returns receiver; lookup case-insensitive; number/string/string[] stored until transmission; invalid names/values throw."
  },
  "caveats": [
    "Preserve Node stream/EventEmitter, host ordering, header state, and network/TLS behavior rather than substituting unrelated .NET HTTP defaults."
  ],
  "references": [
    "https://nodejs.org/api/http.html",
    "rules/events_streams/writable-write.json"
  ],
  "tests": [
    {
      "name": "normal semantics",
      "input": "res.setHeader('X','v')===res",
      "expected_behavior": "true"
    },
    {
      "name": "edge semantics",
      "input": "res.setHeader('X','a\\nb')",
      "expected_behavior": "throws TypeError"
    }
  ]
}
