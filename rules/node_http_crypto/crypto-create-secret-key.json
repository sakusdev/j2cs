{
  "id": "node.crypto.create-secret-key",
  "category": "node_http_crypto",
  "summary": "Create a secret KeyObject from key material.",
  "source": {
    "pattern": "$crypto.createSecretKey($key, $encoding?)",
    "requirements": {
      "module_binding": "node:crypto builtin",
      "builtin_not_shadowed": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "NodeCrypto.CreateSecretKey",
    "template": "NodeCrypto.CreateSecretKey($key, $encoding)",
    "notes": "Returns type 'secret'; input bytes are captured independently from later source-buffer mutation; string encoding is explicit/defaulted per Node."
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "notes": "Returns type 'secret'; input bytes are captured independently from later source-buffer mutation; string encoding is explicit/defaulted per Node."
  },
  "caveats": [
    "Preserve Node Buffer/ArrayBuffer, encoding, validation, provider, and callback semantics rather than only cryptographic mathematical output."
  ],
  "references": [
    "https://nodejs.org/api/crypto.html",
    "rules/binary/buffer-from.json"
  ],
  "tests": [
    {
      "name": "normal semantics",
      "input": "crypto.createSecretKey(Buffer.from([1])).type",
      "expected_behavior": "secret"
    },
    {
      "name": "edge semantics",
      "input": "mutate source Buffer after createSecretKey",
      "expected_behavior": "exported key bytes remain original"
    }
  ]
}
