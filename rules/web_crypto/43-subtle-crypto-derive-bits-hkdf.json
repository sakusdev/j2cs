{
  "id": "web.subtle-crypto.derive-bits.hkdf",
  "category": "web_crypto",
  "summary": "Derive HKDF bits.",
  "source": {
    "pattern": "subtle.deriveBits({name:'HKDF',hash,salt,info},key,length)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.DeriveBits",
    "template": "JsWebCrypto.DeriveBits(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "salt/info are BufferSources and output length is algorithm-limited."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.deriveBits({name:'HKDF',hash,salt,info},key,length)",
      "expected_behavior": "Derive HKDF bits."
    },
    {
      "name": "semantic trap",
      "input": "subtle.deriveBits({name:'HKDF',hash,salt,info},key,length)",
      "expected_behavior": "salt/info are BufferSources and output length is algorithm-limited."
    }
  ]
}
