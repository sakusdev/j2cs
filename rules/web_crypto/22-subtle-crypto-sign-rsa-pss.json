{
  "id": "web.subtle-crypto.sign.rsa-pss",
  "category": "web_crypto",
  "summary": "Sign RSA-PSS with explicit saltLength.",
  "source": {
    "pattern": "subtle.sign({name:'RSA-PSS',saltLength},key,data)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.Sign",
    "template": "JsWebCrypto.Sign(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "saltLength is operation input and invalid sizes reject."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.sign({name:'RSA-PSS',saltLength},key,data)",
      "expected_behavior": "Sign RSA-PSS with explicit saltLength."
    },
    {
      "name": "semantic trap",
      "input": "subtle.sign({name:'RSA-PSS',saltLength},key,data)",
      "expected_behavior": "saltLength is operation input and invalid sizes reject."
    }
  ]
}
