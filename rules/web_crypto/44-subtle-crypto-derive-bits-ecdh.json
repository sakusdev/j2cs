{
  "id": "web.subtle-crypto.derive-bits.ecdh",
  "category": "web_crypto",
  "summary": "Derive ECDH shared secret.",
  "source": {
    "pattern": "subtle.deriveBits({name:'ECDH',public:peer},privateKey,length)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.DeriveBits",
    "template": "JsWebCrypto.DeriveBits(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Key curves/types must match and base key requires deriveBits usage."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.deriveBits({name:'ECDH',public:peer},privateKey,length)",
      "expected_behavior": "Derive ECDH shared secret."
    },
    {
      "name": "semantic trap",
      "input": "subtle.deriveBits({name:'ECDH',public:peer},privateKey,length)",
      "expected_behavior": "Key curves/types must match and base key requires deriveBits usage."
    }
  ]
}
