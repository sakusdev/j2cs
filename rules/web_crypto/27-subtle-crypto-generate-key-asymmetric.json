{
  "id": "web.subtle-crypto.generate-key.asymmetric",
  "category": "web_crypto",
  "summary": "Generate public/private CryptoKeyPair.",
  "source": {
    "pattern": "subtle.generateKey(rsaOrEcParams,extractable,usages)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.GenerateKey",
    "template": "JsWebCrypto.GenerateKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Public/private usages are filtered/validated per algorithm."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.generateKey(rsaOrEcParams,extractable,usages)",
      "expected_behavior": "Generate public/private CryptoKeyPair."
    },
    {
      "name": "semantic trap",
      "input": "subtle.generateKey(rsaOrEcParams,extractable,usages)",
      "expected_behavior": "Public/private usages are filtered/validated per algorithm."
    }
  ]
}
