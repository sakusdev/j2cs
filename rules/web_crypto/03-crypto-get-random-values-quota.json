{
  "id": "web.crypto.get-random-values.quota",
  "category": "web_crypto",
  "summary": "Enforce WebCrypto random-value byte quota.",
  "source": {
    "pattern": "crypto.getRandomValues(array)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.GetRandomValues",
    "template": "JsWebCrypto.GetRandomValues(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "byteLength above 65536 throws QuotaExceededError."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "crypto.getRandomValues(array)",
      "expected_behavior": "Enforce WebCrypto random-value byte quota."
    },
    {
      "name": "semantic trap",
      "input": "crypto.getRandomValues(array)",
      "expected_behavior": "byteLength above 65536 throws QuotaExceededError."
    }
  ]
}
