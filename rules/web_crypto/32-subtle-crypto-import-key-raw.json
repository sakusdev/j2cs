{
  "id": "web.subtle-crypto.import-key.raw",
  "category": "web_crypto",
  "summary": "Import raw symmetric/EC key material where supported.",
  "source": {
    "pattern": "subtle.importKey('raw',data,algorithm,extractable,usages)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.ImportKey",
    "template": "JsWebCrypto.ImportKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "BufferSource bytes are parsed per selected algorithm."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.importKey('raw',data,algorithm,extractable,usages)",
      "expected_behavior": "Import raw symmetric/EC key material where supported."
    },
    {
      "name": "semantic trap",
      "input": "subtle.importKey('raw',data,algorithm,extractable,usages)",
      "expected_behavior": "BufferSource bytes are parsed per selected algorithm."
    }
  ]
}
