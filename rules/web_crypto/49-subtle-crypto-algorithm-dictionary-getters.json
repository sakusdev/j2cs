{
  "id": "web.subtle-crypto.algorithm-dictionary-getters",
  "category": "web_crypto",
  "summary": "Preserve observable JS property access while normalizing algorithm dictionaries.",
  "source": {
    "pattern": "subtle.operation(algorithmObject,...)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "runtime",
    "template": "JsRuntime.JsWebCrypto.NormalizeAlgorithmDynamic(/* lowered operands */)"
  },
  "strategy": "runtime",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Getters/proxies can execute/throw during normalization and required members are read per Web IDL."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.operation(algorithmObject,...)",
      "expected_behavior": "Preserve observable JS property access while normalizing algorithm dictionaries."
    },
    {
      "name": "semantic trap",
      "input": "subtle.operation(algorithmObject,...)",
      "expected_behavior": "Getters/proxies can execute/throw during normalization and required members are read per Web IDL."
    }
  ]
}
