{
  "id": "web.subtle-crypto.generate-key.hmac",
  "category": "web_crypto",
  "summary": "Generate HMAC secret key.",
  "source": {
    "pattern": "subtle.generateKey({name:'HMAC',hash,length},...)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.GenerateKey",
    "template": "JsWebCrypto.GenerateKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Default/explicit HMAC length follows algorithm rules."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.generateKey({name:'HMAC',hash,length},...)",
      "expected_behavior": "Generate HMAC secret key."
    },
    {
      "name": "semantic trap",
      "input": "subtle.generateKey({name:'HMAC',hash,length},...)",
      "expected_behavior": "Default/explicit HMAC length follows algorithm rules."
    }
  ]
}
