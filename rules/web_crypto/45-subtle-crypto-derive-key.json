{
  "id": "web.subtle-crypto.derive-key",
  "category": "web_crypto",
  "summary": "Derive a new CryptoKey with requested metadata.",
  "source": {
    "pattern": "subtle.deriveKey(algorithm,baseKey,derivedKeyType,extractable,keyUsages)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.DeriveKey",
    "template": "JsWebCrypto.DeriveKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Semantics combine derivation and import-like key construction/usage validation."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.deriveKey(algorithm,baseKey,derivedKeyType,extractable,keyUsages)",
      "expected_behavior": "Derive a new CryptoKey with requested metadata."
    },
    {
      "name": "semantic trap",
      "input": "subtle.deriveKey(algorithm,baseKey,derivedKeyType,extractable,keyUsages)",
      "expected_behavior": "Semantics combine derivation and import-like key construction/usage validation."
    }
  ]
}
