{
  "id": "web.subtle-crypto.generate-key",
  "category": "web_crypto",
  "summary": "Generate symmetric CryptoKey or asymmetric CryptoKeyPair.",
  "source": {
    "pattern": "subtle.generateKey(algorithm,extractable,keyUsages)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.GenerateKey",
    "template": "JsWebCrypto.GenerateKey(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Generated key metadata exactly reflects normalized algorithm, extractability and valid usages."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.generateKey(algorithm,extractable,keyUsages)",
      "expected_behavior": "Generate symmetric CryptoKey or asymmetric CryptoKeyPair."
    },
    {
      "name": "semantic trap",
      "input": "subtle.generateKey(algorithm,extractable,keyUsages)",
      "expected_behavior": "Generated key metadata exactly reflects normalized algorithm, extractability and valid usages."
    }
  ]
}
