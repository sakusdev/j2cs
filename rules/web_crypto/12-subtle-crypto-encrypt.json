{
  "id": "web.subtle-crypto.encrypt",
  "category": "web_crypto",
  "summary": "Encrypt BufferSource with a CryptoKey and algorithm dictionary.",
  "source": {
    "pattern": "subtle.encrypt(algorithm,key,data)",
    "requirements": {
      "intrinsic_web_crypto_api": true,
      "builtin_not_overridden": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "JsWebCrypto.Encrypt",
    "template": "JsWebCrypto.Encrypt(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "webcrypto_contract": true,
    "binary": "reuse BINARY BufferSource semantics",
    "async": "reuse ASYNC Promise job/settlement semantics"
  },
  "caveats": [
    "Returns Promise<ArrayBuffer>; key algorithm/usages and operation algorithm are validated."
  ],
  "references": [
    "https://www.w3.org/TR/WebCryptoAPI/"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "subtle.encrypt(algorithm,key,data)",
      "expected_behavior": "Encrypt BufferSource with a CryptoKey and algorithm dictionary."
    },
    {
      "name": "semantic trap",
      "input": "subtle.encrypt(algorithm,key,data)",
      "expected_behavior": "Returns Promise<ArrayBuffer>; key algorithm/usages and operation algorithm are validated."
    }
  ]
}
