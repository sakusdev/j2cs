{
  "id": "electron.webrequest.on-before-request",
  "category": "electron.webrequest",
  "summary": "Register the onBeforeRequest Session network interception stage.",
  "source": {
    "pattern": "$webRequest.onBeforeRequest($filter?, $listener)",
    "requirements": {
      "electron_binding_proven": true,
      "builtin_not_overridden": true,
      "main_process": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "ElectronCompat.WebRequest.onBeforeRequest",
    "template": "ElectronCompat.WebRequest.onBeforeRequest(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "normal": "before request; callback may cancel or redirect",
    "edge": "only the last listener registered for this event is used; null unsubscribes"
  },
  "caveats": [
    "Filters are URL-pattern based and listener replacement is per WebRequest event."
  ],
  "references": [
    "https://www.electronjs.org/docs/latest/api/web-request"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "session.defaultSession.webRequest.onBeforeRequest({urls:['https://*/*']},()=>{})",
      "expected_behavior": "before request; callback may cancel or redirect"
    },
    {
      "name": "edge semantics",
      "input": "session.defaultSession.webRequest.onBeforeRequest(null)",
      "expected_behavior": "only the last listener registered for this event is used; null unsubscribes"
    }
  ]
}
