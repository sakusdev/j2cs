{
  "id": "electron.webrequest.on-before-redirect",
  "category": "electron.webrequest",
  "summary": "Register the onBeforeRedirect Session network interception stage.",
  "source": {
    "pattern": "$webRequest.onBeforeRedirect($filter?, $listener)",
    "requirements": {
      "electron_binding_proven": true,
      "builtin_not_overridden": true,
      "main_process": true
    }
  },
  "target": {
    "kind": "helper",
    "helper": "ElectronCompat.WebRequest.onBeforeRedirect",
    "template": "ElectronCompat.WebRequest.onBeforeRedirect(/* lowered operands */)"
  },
  "strategy": "helper",
  "confidence": "high",
  "semantics": {
    "normal": "before a server redirect is followed",
    "edge": "only the last listener registered for this event is used; null unsubscribes"
  },
  "caveats": [
    "Filters are URL-pattern based and listener replacement is per WebRequest event."
  ],
  "references": [
    "https://www.electronjs.org/docs/latest/api/web-request"
  ],
  "tests": [
    {
      "name": "normal behavior",
      "input": "session.defaultSession.webRequest.onBeforeRedirect({urls:['https://*/*']},()=>{})",
      "expected_behavior": "before a server redirect is followed"
    },
    {
      "name": "edge semantics",
      "input": "session.defaultSession.webRequest.onBeforeRedirect(null)",
      "expected_behavior": "only the last listener registered for this event is used; null unsubscribes"
    }
  ]
}
